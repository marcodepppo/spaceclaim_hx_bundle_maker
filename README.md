This is a SpaceClaim script. It is used to parametrically generate the bundle 
of pipes that constitues the inner body of a heat exchanger. 

PREREQUISITES:

a model with a cylindric base to use as tubesheet component

USE:
- import the script in SpaceClaim
- set the script parameters
- select the base of the cylinder base and run the script
